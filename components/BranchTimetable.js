import React from 'react';
import GenerateTimetable from '../screens/GenerateTimetable';

const BranchTimetable = ({ branch }) => {
  return <GenerateTimetable branch={branch} />;
};

export default BranchTimetable;
